# =============================================================================
# CURRICULUM GENERATOR APP - FRONTEND ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains all environment variables needed for the Next.js frontend.
# Copy this file to .env.local and update with your actual values.
#
# For Render deployment, set these variables in the Render dashboard.
# For local development, use .env.local (not tracked by git).
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Backend API URL
# Development: http://localhost:4000
# Production (Render): https://your-api-service.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:4000

# -----------------------------------------------------------------------------
# Authentication - Auth0
# -----------------------------------------------------------------------------
# Auth0 domain (same as backend)
# Example: your-tenant.auth0.com or your-tenant.us.auth0.com
NEXT_PUBLIC_AUTH0_DOMAIN=your-auth0-domain.auth0.com

# Auth0 Client ID for the frontend application
# Get this from Auth0 Dashboard → Applications → Your App → Settings
NEXT_PUBLIC_AUTH0_CLIENT_ID=your-auth0-client-id

# Auth0 Audience (API identifier - same as backend AUTH0_AUDIENCE)
# Get this from Auth0 Dashboard → APIs → Your API → Settings
NEXT_PUBLIC_AUTH0_AUDIENCE=your-api-audience

# Auth0 Redirect URI (where Auth0 redirects after login)
# Development: http://localhost:3000/callback
# Production: https://your-frontend.onrender.com/callback
NEXT_PUBLIC_AUTH0_REDIRECT_URI=http://localhost:3000/callback

# =============================================================================
# RENDER DEPLOYMENT NOTES
# =============================================================================
# 
# When deploying to Render:
# 1. Set all variables in Render dashboard (Settings → Environment)
# 2. NEXT_PUBLIC_API_URL: Set to your backend API URL on Render
#    Example: https://curriculum-api.onrender.com
# 3. Configure Auth0:
#    - Add Render frontend URL to Allowed Callback URLs
#    - Add Render frontend URL to Allowed Logout URLs
#    - Add Render frontend URL to Allowed Web Origins
# 4. NEXT_PUBLIC_AUTH0_REDIRECT_URI: Set to your frontend URL + /callback
#    Example: https://curriculum-app.onrender.com/callback
# 5. Ensure CORS_ORIGINS in backend includes your frontend URL
#
# Note: All NEXT_PUBLIC_* variables are embedded in the client-side bundle
# and are publicly visible. Never put secrets in NEXT_PUBLIC_* variables.
#
# =============================================================================
