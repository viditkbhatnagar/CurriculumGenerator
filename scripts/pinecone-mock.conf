events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        
        location / {
            return 200 '{"status": "mock", "message": "Pinecone mock service for local development"}';
            add_header Content-Type application/json;
        }
    }
}
